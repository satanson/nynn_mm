#!/bin/bash
prodHost=$1
prodPort=$2
vtxnoBeg=$3
vtxnoEnd=$4

file=$5
vtxno=$vtxnoBeg

while [ $vtxno -lt $vtxnoEnd ];do
	bin/prod_push ${prodHost} ${prodPort} ${vtxno} <$file
	echo "bin/prod_push ${prodHost} ${prodPort} ${vtxno} <$file"
	vtxno=$(($vtxno+1))
done
