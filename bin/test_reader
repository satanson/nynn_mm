#!/bin/bash
prodHost=$1
prodPort=$2
vtxnoBeg=$3
vtxnoEnd=`expr $4 - 1`

file=$5

for vtxno in `eval "echo {$vtxnoBeg..$vtxnoEnd}"`;do
	bin/agent_shift ${prodHost} ${prodPort} ${vtxno} >"${file}-${vtxno}.txt"
done

