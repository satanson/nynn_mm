#include "zms_client.hpp"
int main()
{
  /*  init();
    read_balance();
    print_bs();
    int file[4]={1,6,7,8};
    distribute(file,4);
    print_bs();
    print_bs_t();
    write_balance();
    parse_dir("test");
    for(int i=0;i<file_len;i++)
		cout<<files[i]<<endl;
	//cout<<suf_len<<"  "<<suffix<<endl;
    cout<<prefix<<" "<<suffix<<endl;
    cout<<prefix.length()<<" "<<suffix.length()<<endl;
    get_filename(123);
    return 1;*/
   	zms_client("test");
}
